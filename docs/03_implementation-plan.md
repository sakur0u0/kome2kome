# 03. 実装計画

2026-09-06 承認。進捗は下記チェックボックス（Phase 1〜3 完了、Phase 4 は素材受領待ち）。

## Phase 1 — 事実の修正（現行 LP の誤りを即時解消）

- [x] 3 言語辞書を書き換え（土鍋・和食ダイニング・隠れ家レストラン表現の全削除）
- [x] 店名・肩書きを「米と米 京都祇園店【お米の専門店】/ 米粉クレープ × 体験型お米モンブラン」へ
- [x] CTA を「品書きを見る」「Instagram」「Google マップで経路」に変更（予約導線を撤去）
- [x] パレットに 和紙 / 墨 / 薄墨 / 朱 を追加、`site.config.ts` に店舗情報（住所・営業時間・SNS・決済）を集約
- [x] ロゴを実物ベースの SVG に置換（未受領の場合はトレース版）

## Phase 2 — セクション再構成（9 章）

- [x] 壱 宣言 / 弐 こだわり（3 柱）/ 参 品書き / 四 体験 / 五 お米 / 六 空気 / 七 道 の 7 セクションを新規作成
- [x] 既存 Concept / Experience / Reservation を廃止し、内容を上記へ吸収
- [x] 章ごとの背景トーン（和紙 → 墨 → 漆黒）— 各章の `data-tone` + `InkEdge` の継ぎ目で実装（`useSceneProgress` は不要と判断、Header は `useHeaderTone` で追従）
- [x] Header ナビを 7 章に合わせて再編（モバイルは章番号付きリスト）

## Phase 3 — 日本的アニメーション基盤

- [x] `InkReveal`（墨の滲みマスク）
- [x] `BrushKanji`（KanjiVG 筆順パスの一画描き）— 米・極・静・縁・道 を用意
- [x] `ColorAwakening`（grayscale → color のスクロール連動）
- [x] `SealStamp`（朱の落款スタンプ）
- [x] `ScrollGallery`（巻物の横スクロール）→ `MenuSection` の `ScrollTrack` として実装
- [x] ~~`ScrubVideo`~~ → 決定事項により静止画 + プロシージャル線描 `MonbranDrawing` に変更
- [x] `GoldLeafCanvas`（金箔パーティクル、スクロール進捗で密度制御、reduced-motion で停止）
- [x] `NorenTransition`（暖簾）— モバイルメニュー / 言語切替
- [x] `VerticalType`（縦書き 1 字 stagger）
- [x] 和紙テクスチャ・カスタムカーソル

## Phase 4 — 素材差し替えと仕上げ

- [ ] 実写真・ロゴの反映（受領後）— 仮素材は `public/img/placeholder/` にローカル同梱済み
- [ ] お米の商品情報反映（受領後）
- [ ] 3 言語コピーの最終校正（英・中はネイティブチェック推奨）
- [x] 3 言語 × 3 画面幅のスクリーンショット QA（`docs/04_qa-report.md`）／ Lighthouse は実素材差し替え後に実施
- [x] JSON-LD（FoodEstablishment）追加 ／ OG 画像は実写真受領後

## 想定ボリューム

| Phase | 新規 / 変更ファイル数（目安） |
| --- | --- |
| 1 | 約 8 |
| 2 | 約 12 |
| 3 | 約 12 |
| 4 | 約 6 |

## 決定事項（2026-09-06 確認済み）

| 論点 | 決定 |
| --- | --- |
| 色の物語 | **Plan A**: ヒーロー後に「和紙 × 墨」の明るい幕 → 朱 → 漆黒 × 金 |
| 素材 | **仮素材（Unsplash）で先に形を作る**。実写真・ロゴ SVG・お米情報は受領後に差し替え |
| 四 体験 | 動画は使わず、**静止画 + 墨マスク + プロシージャル演出**（お米クリームの線描アニメーション） |
| 主 CTA | **Google マップで経路**（副: Instagram） |
| 地図 | 埋め込みは行わず、墨線の簡易地図 + Google マップへの外部リンク |

## 受領待ち（実装はプレースホルダーで進行）

- 販売しているお米の品種・産地・価格・パッケージ写真 → 「品種・価格は店頭 / Instagram にて」表記で仮置き
- 店名「米と米」の由来 → 「食べる米と、持ち帰る米。ふたつの米。」を仮の解釈として掲載（要校正）
- 定休日 → 「Instagram にてご確認ください」で仮置き
- ロゴ SVG → Instagram のロゴを参考に HTML/SVG で再現（白丸・筆文字「米」・朱の落款）
- 商品写真・店内写真・動画
